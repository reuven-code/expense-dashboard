import { Router } from 'express';
import { Firestore } from 'firebase-admin/firestore';

export const adminRouter = (db: Firestore) => {
  const router = Router();
  // TODO: Implement admin routes
  return router;
};
