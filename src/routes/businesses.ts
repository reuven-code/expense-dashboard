import { Router } from 'express';
import { Firestore } from 'firebase-admin/firestore';

export const businessRouter = (db: Firestore) => {
  const router = Router();
  // TODO: Implement business routes
  return router;
};
